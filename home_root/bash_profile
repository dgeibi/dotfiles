#
# ~/.bash_profile
#

source ~/.profile

[[ -f ~/.bashrc ]] && source ~/.bashrc

systemctl --user import-environment PATH

if [[ -z $DISPLAY ]] && [[ $(tty) = /dev/tty1 ]] && command -v mate-session &> /dev/null
then
    startx
fi
